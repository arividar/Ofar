@page
@model Ofar.Pages.PhotographersModel
<div class="row">
    <h1 class="display-2">Photographers</h1>
    <table class="table">
        <thead class="thead-inverse">
            <tr><th>Famous Photographers</th></tr>
        </thead>
        <tbody>
            @foreach (string name in Model.Photographers)
            {
                <tr><td>@name</td></tr>
            }
        </tbody>
    </table>
</div>